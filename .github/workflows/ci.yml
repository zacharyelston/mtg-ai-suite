name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  # ============================================
  # Rust checks
  # ============================================
  rust-check:
    name: Rust Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy lints
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo test --all-features

      - name: Build release
        run: cargo build --release

  # ============================================
  # TypeScript checks (only if client exists)
  # ============================================
  typescript-check:
    name: TypeScript Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check if client exists
        id: check_client
        run: |
          if [ -d "client" ] && [ -f "client/package.json" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup Node.js
        if: steps.check_client.outputs.exists == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        if: steps.check_client.outputs.exists == 'true'
        working-directory: ./client
        run: npm ci

      - name: Type check
        if: steps.check_client.outputs.exists == 'true'
        working-directory: ./client
        run: npm run type-check

      - name: Lint
        if: steps.check_client.outputs.exists == 'true'
        working-directory: ./client
        run: npm run lint

      - name: Run tests
        if: steps.check_client.outputs.exists == 'true'
        working-directory: ./client
        run: npm run test

      - name: Build
        if: steps.check_client.outputs.exists == 'true'
        working-directory: ./client
        run: npm run build

      - name: Skip message
        if: steps.check_client.outputs.exists == 'false'
        run: echo "Client directory not found, skipping TypeScript checks"

  # ============================================
  # Integration tests (SQLite for CI simplicity)
  # ============================================
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [rust-check, typescript-check]
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install SQLite
        run: sudo apt-get update && sudo apt-get install -y sqlite3 libsqlite3-dev

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Run integration tests
        run: cargo test --all --features integration
        env:
          DATABASE_URL: "sqlite::memory:"
          REDIS_URL: redis://localhost:6379

  # ============================================
  # WASM build
  # ============================================
  wasm:
    name: WASM Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: cargo install wasm-pack

      - name: Build WASM
        run: wasm-pack build crates/mtg-wasm --target web

      - name: Upload WASM artifact
        uses: actions/upload-artifact@v4
        with:
          name: wasm-pkg
          path: crates/mtg-wasm/pkg/
